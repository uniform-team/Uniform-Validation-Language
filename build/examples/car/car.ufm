@filled: /"."/;
@integer: /"^[0-9]{4}$"/;

$("#rootForm") {
	valid:
		not $("#hasCar") or
		$("#carForm") is valid;
}

$("#carForm") {
	valid:
	    $("#make") matches @filled and
	    $("#model") matches @filled and
	    $("#year") matches @integer;
}