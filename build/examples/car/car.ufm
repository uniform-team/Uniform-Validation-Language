@filled: /"."/;
@year: /"^[0-9]{4}$"/;

valid: not hasCar or @carForm.valid;

@carForm {
    valid: if make.valid then
        true
    elif model.valid then
        true
    else
        false
    end;

    make {
        valid: make matches @filled;
    }

    model {
        valid: model matches @filled;
    }

    year {
        valid: year matches @year;
    }
}