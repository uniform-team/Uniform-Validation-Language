@integer: /"someregex"/;

$("#rootForm") {
	valid:
	       not $("#hasCar") is valid or
	       $("#carForm") is valid;
}

$("#carForm") {
	valid:
	    $("#make") is valid and
	    $("#model") is valid and
	    $("#year") matches @integer;
}